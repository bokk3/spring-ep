name = "artist-portfolio"
compatibility_date = "2024-01-01"

[site]
bucket = "./dist"

# Build configuration
[build]
command = "npm run build"
cwd = "."
watch_dir = "src"

# Environment variables (if needed)
[env.production]
name = "artist-portfolio-production"

[env.preview]
name = "artist-portfolio-preview"
